{
  "name": "@redhat/upkeep",
  "version": "0.0.0",
  "description": "Configurable reporting on repository dependencies",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.corp.redhat.com:akimberl/upkeep.git"
  },
  "author": "Drew Kimberly",
  "license": "UNLICENSED",
  "dependencies": {
    "gts": "^2.0.0-alpha.9"
  },
  "devDependencies": {
    "gts": "^2.0.0-alpha.9",
    "typescript": "~3.8.0",
    "@types/node": "^10.0.3"
  }
}
